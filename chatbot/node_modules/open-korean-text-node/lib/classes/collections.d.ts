import { AbstractJavaClass } from './abstract';
export declare class Collections<T> extends AbstractJavaClass {
    static className: string;
    map<R>(mapFunc: (item: T, index?: number) => R): R[];
}
export declare class ArrayList<T> extends Collections<T> {
    static className: string;
    constructor(values?: T[]);
    add(item: T): any;
    addSync(item: T): any;
    addAll(items: T[]): Promise<any[]>;
    addAllSync(items: T[]): void;
}
export declare class Seq<T> extends Collections<T> {
    static className: string;
}
